import '@testing-library/jest-dom/vitest';

// This file is used to set up global configurations for tests.
// By importing '@testing-library/jest-dom/vitest', we extend Vitest's `expect`
// with helpful matchers for asserting on DOM nodes.
// For example, you can use `expect(element).toBeInTheDocument()`.

// This file should be referenced in the `setupFiles` option of your vitest.config.ts.